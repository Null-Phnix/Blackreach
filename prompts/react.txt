GOAL: {goal}

STATE:
- Page: {title}
- URL: {url}
- Downloads: {download_count}
{extra_context}

ELEMENTS:
{elements}

ACTIONS:
- type: Search (e.g., {{"action":"type","args":{{"selector":"input","text":"query"}}}})
- navigate: Go to URL (e.g., {{"action":"navigate","args":{{"url":"https://..."}}}})
- click: Click by text (e.g., {{"action":"click","args":{{"text":"link text"}}}})
- download: Download file (e.g., {{"action":"download","args":{{"url":"https://..."}}}})
- done: ONLY when downloads >= goal target

CRITICAL RULES:
1. If goal says "find/get/download X" - you MUST actually download something before saying done
2. If downloads = 0 and goal needs files - you are NOT done, keep searching
3. On search pages (Google/Wikipedia/etc) - use TYPE to search for what you need
4. When you see DOWNLOAD links with .epub/.pdf/.zip - use DOWNLOAD action
5. When you see DETAIL PAGE links - NAVIGATE to them first

STRATEGY for "{goal}":
- If on generic page: search for it using TYPE
- If on results page: navigate to a relevant result
- If on content page: find and use DOWNLOAD link
- ONLY use done when you have actually downloaded {download_count} files (need >= 1)

OUTPUT (JSON only, no other text):
{{"thought":"what I'm doing","action":"ACTION","args":{{...}}}}
